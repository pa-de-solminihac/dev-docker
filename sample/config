# chemins
DEVDOCKER_VM="devdockervm"
DEVDOCKER_REPOSITORY="quai2.quai13.com:5000"
DEVDOCKER_IMAGE="$DEVDOCKER_REPOSITORY/devdocker"
DEVDOCKER_AUTOUPDATE="1"
DOCKERSITE_ROOT="/c/Users/...../path/to/dockersite"
SSH_DIR="$HOME/.ssh" # note that if you need to sudo, $HOME will be /root/
MYSQL_FORCED_ROOT_PASSWORD="" # if left empty, a different random mysql root password will be forced at every startup
